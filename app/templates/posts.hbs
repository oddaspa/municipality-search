<h1> This is our post route </h1>

<!--
{{#each model as  |post|}}
  <h3>Name: {{ post.description }}</h3>
  <p> Code: {{ post.label }} </p>
  <p> By: {{ post.lang }}</p>
  <p> Status: {{ post.status }} </p>
  <p> Date Subitted: {{post.dateSubmitted}} </p>
{{/each}}
-->
<div class="box-set">

  <ListFilter class="box"
    @filter={{action "filterByName"}}
    as |filteredResults|
    >
    <ul class="results">
      {{#each filteredResults as |resUnit|}}
        <li><PostListing @post={{resUnit}} /></li>
      {{/each}}
    </ul>
  </ListFilter>

  <!-- RadioButtons
  <RadioButtons class="box"/>
   -->
</div>
{{outlet}}
